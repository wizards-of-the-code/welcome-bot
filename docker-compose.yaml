version: '3'

services:
  bot:
    build: .
    volumes:
      - ${MEDIA_PATH}:${MEDIA_PATH}
      - /logs:${LOGS_PATH}
      - /sessions:${SESSIONS_PATH}
    command: node ./dist/app.js
    env_file:
      - .env
